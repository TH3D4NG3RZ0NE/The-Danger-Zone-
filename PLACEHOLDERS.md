# Owner placeholders

Before publishing with final owner details, update `src/content/site-content.ts`.

- `contact.email` — TODO(owner): live team contact email.
- `contact.phone` — TODO(owner): live team phone number.
- `location` — TODO(owner): confirmed team/gym address.
- `trainingTimes` — TODO(owner): confirmed training schedule.
- `socials.facebook` — TODO(owner): Facebook URL.
- `socials.instagram` — TODO(owner): Instagram URL.
- `socials.youtube` — TODO(owner): YouTube URL.

The schedule/events section intentionally remains an empty state until real dates, opponents, or events are supplied.
